let config = {}

config.username = '' // GitHub username
config.password = '' // GitHub password
config.privateKey = '' // GitHub privatekey
config.repoName = '' // GitHub repo name, it can be an existing one. If it does not exist, a new one in this name will be created
config.repoDesc = '' // Description for repo in case a new one is created
config.dir = '' // Directory of local repo. It may or may not exist, if it doesn't a new one will be created

module.exports = config